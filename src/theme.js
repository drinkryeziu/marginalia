// Design tokens for My Little Secret Diary. Single source of truth for color + type.
// Palette: cool oatmeal paper, fountain-pen indigo ink, antique-brass accent.

export const C = {
  paper: "#EDEBE4",   // app background / shelf
  page: "#F6F5F0",    // writing surface / cards / inputs
  ink: "#26263A",     // primary text (softer than black)
  inkSoft: "#5B5B70", // secondary text
  faint: "#9A9AAB",   // captions / placeholders
  brass: "#A5873F",   // accent
  brassDeep: "#8A6F2E",
  line: "#DEDBD1",    // hairlines / borders
  danger: "#9E4B45",
};

export const font = {
  display: "'Fraunces', Georgia, serif", // headings, dates, logo
  body: "'Newsreader', Georgia, serif",  // diary prose, body copy
  ui: "'Fraunces', system-ui, sans-serif", // labels, buttons
};
